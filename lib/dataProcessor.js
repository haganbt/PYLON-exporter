'use strict';

var log = require('../utils/logger')
    ;

/**
 * process - PYLON response data
 *
 * @param inData - obj
 * @param cb - obj - err
 */
var process = function process(inData, cb) {

    //cb(new Error("Foo"));
    cb(null);
};

exports.process = process;